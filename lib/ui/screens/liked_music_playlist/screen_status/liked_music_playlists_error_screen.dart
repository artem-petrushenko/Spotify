import 'package:flutter/material.dart';

class LikedMusicPlaylistsErrorScreen extends StatelessWidget {
  const LikedMusicPlaylistsErrorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CustomScrollView();
  }
}
