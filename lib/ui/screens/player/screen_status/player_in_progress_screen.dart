import 'package:flutter/material.dart';

class PlayerInProgressScreen extends StatelessWidget {
  const PlayerInProgressScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
