import 'package:flutter/material.dart';

class ArtistErrorScreen extends StatelessWidget {
  const ArtistErrorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
