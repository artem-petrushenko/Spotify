import 'package:flutter/material.dart';

class TransferPlaybackErrorScreen extends StatelessWidget {
  const TransferPlaybackErrorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('ERROR'),
    );
  }
}
