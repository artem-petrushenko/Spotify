import 'package:flutter/material.dart';

class TransferPlaybackInProgressScreen extends StatelessWidget {
  const TransferPlaybackInProgressScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('PROGRESS'),
    );
  }
}
